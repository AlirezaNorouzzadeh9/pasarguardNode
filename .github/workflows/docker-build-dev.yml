name: Build and Push Docker Image (All Branches)

# Upstream's workflow: pushes to the `pasarguard/*` Docker Hub repo, whose
# secrets this fork doesn't have, so it failed on every single push and buried
# the signal from the workflow we actually use (docker-ghcr.yml). Manual-only
# here — re-enable the push trigger if this fork ever gets Docker Hub creds.
on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE: pasarguard/${{ github.event.repository.name }}:${{ github.ref_name }}
  GHCR_IMAGE: ghcr.io/pasarguard/${{ github.event.repository.name }}:${{ github.ref_name }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
  
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push multi-arch Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{env.GHCR_IMAGE}}
            ${{env.IMAGE}}
          cache-from: type=gha
          cache-to: type=gha,mode=max
